Generate integration tests for {scenario[description]} in Spring PetClinic. Follow this reasoning framework:

1. CONTEXT ANALYSIS:
   - Relevant classes: {scenario[context_files]}
   - Key integration points: {scenario[integration_points]}

2. TEST DESIGN:
   - Happy path scenario:
     * Preconditions: 
     * Actions: 
     * Assertions:
   - Edge cases:
     * Case 1: 
     * Case 2:

3. IMPLEMENTATION:
   - Required mocks: 
   - Test data setup:
   - Verification strategy:

4.  **FINAL CODE GENERATION:**
    * Based on the plan above, generate the complete Java test class.
    * **Crucially, ensure the following:**
        * All necessary import statements from `org.springframework`, `org.junit.jupiter`, and `org.mockito` are included.
        * The code uses JUnit 5 annotations (`@Test`, `@BeforeEach`, `@ExtendWith(MockitoExtension.class)`).
        * All mocks are correctly initialized.