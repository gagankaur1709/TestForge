[CONSTRAINTS]
1. Max 2 mocks per test class
2. Cyclomatic complexity < 6
3. Test methods < 15 lines
4. Use JUnit 5 only
5. Assertion density = 1-3 per test

[SCENARIO] {scenario.description}
[CONTEXT] {scenario.context_files}

[REQUIREMENTS]
- Generate tests that satisfy ALL constraints
- Reject any solutions violating constraints
- Include constraint compliance comments

[EXAMPLE]
// CONSTRAINT: 1 mock (OwnerRepository)
@MockBean OwnerRepository ownerRepository;

@Test  // CONSTRAINT: CC=3, lines=8
void shouldReturn404_whenOwnerNotFound() {
    given(ownerRepository.findById(any())).willReturn(Optional.empty());
    
    assertThrows(ResourceNotFoundException.class, 
        () -> ownerController.getOwner(999));
}